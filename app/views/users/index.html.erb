<h1><%= t '.title' %></h1>
<hr>
<% @users.each do |user| %>
  <%= image_tag user.avatar_url, class: 'user-avatar rounded-circle border mb-3' %>
  <p>ユーザ名：<%= link_to user.name, user_path(user) %>  |  理想リンゴ体重：<%= user.decorate.ideal_apple_weight %>個</p>
  <hr>
<% end %>
