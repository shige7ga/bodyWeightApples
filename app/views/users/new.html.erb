<h1>ユーザ登録</h1>

<%= form_with model: @user do |f| %>
  <%= f.label :name, '名前(ニックネーム)' %>
  <%= f.text_field :name %>
  <%= f.label :email, 'メールアドレス' %>
  <%= f.email_field :email %>
  <%= f.submit %>
<% end %>
