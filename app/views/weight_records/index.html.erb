<h1>記録一覧</h1>
<hr>
<% @weight_records.each do |record| %>
  <p>ユーザー名：<%= record.user.name %> | リンゴ体重：<%= record.apple_weight %>個分</p>
  <p>登録日時：<%= record.created_at.strftime("%Y/%m/%d %H:%M") %></p>
  <hr>
<% end %>
