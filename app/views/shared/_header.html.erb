<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to root_path, class: 'navbar-brand ps-2' do %>
      <%= image_tag "apple_header.png", size: "30x30" %>
      リンゴで体重管理
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto text-center mb-2 mb-lg-0">
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to t('.weight_record_index'), weight_records_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.weight_record_new'), new_weight_record_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.users_index'), users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.mypage'), user_path(current_user), class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.logout'), logout_path, data: { turbo_method: :delete }, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('.user_new'), new_user_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.login'), login_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
